# Lab 03: `read` and `write`

## Objective

Read up to 64 bytes from standard input into shellcode-owned storage, then write those bytes to standard output.

## Build and run

```bash
./build.sh
printf 'hello\n' | ./harness
```
